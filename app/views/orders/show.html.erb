<h1>Деталі замовлення</h1> 


<p><strong>Дата:</strong> <%= @order.created_at.strftime('%Y-%m-%d %H:%M:%S') %></p> 

<p><strong>Замовлено:</strong> <%= @order.order_date %></p> 

<p><strong>Сума:</strong> <%= number_to_currency(@order.order_items.sum { |item| item.product.price * item.quantity }) %></p> 

 

<h2>Товари</h2> 

<% @order.order_items.each do |item| %> 

  <div> 

    <p><strong>Назва:</strong> <%= item.product.name %></p> 

    <p><strong>Кількість:</strong> <%= item.quantity %></p> 

    <p><strong>Ціна:</strong> <%= number_to_currency(item.product.price, unit: "₴") %></p> 

  </div> 

  <hr> 

<% end %> 

 

<%= link_to 'Назад до замовлень', orders_path, class: 'btn btn-secondary' %> 